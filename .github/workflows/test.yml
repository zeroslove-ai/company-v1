name: Company v1 tests

on:
  push:
    branches:
      - company/full-feature-transplant-v1
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run regression tests
        shell: bash
        run: |
          set +e
          npm test 2>&1 | tee test-output.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            output=$(tail -n 100 test-output.log)
            output=${output//'%'/'%25'}
            output=${output//$'\r'/'%0D'}
            output=${output//$'\n'/'%0A'}
            echo "::error title=Regression test output::$output"
          fi
          exit "$status"
      - name: API Worker dry-run
        run: npx --yes wrangler deploy --dry-run --config wrangler.api.jsonc
      - name: Frontend Worker dry-run
        run: npx --yes wrangler deploy --dry-run --config wrangler.frontend.jsonc
