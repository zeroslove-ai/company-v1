{
  "contract_id": "company-v1-scene-authority",
  "contract_version": 1,
  "stages": {
    "stage_a": {
      "required_migration_names": ["company_v1_scene_authority_stage_a"],
      "functions": [
        {"name": "company_validate_scene_v1", "identity_arguments": "jsonb, boolean"},
        {"name": "company_bootstrap_scene_v1", "identity_arguments": "jsonb"},
        {"name": "validate_company_save_v1", "identity_arguments": "jsonb"},
        {"name": "reset_company_game", "identity_arguments": "uuid, text"}
      ],
      "probes": [
        "legacy_only_save_accepted",
        "canonical_scene_save_accepted",
        "reset_returns_scene_v1"
      ]
    },
    "stage_b": {
      "required_migration_names": ["company_v1_scene_authority_stage_b"],
      "functions": [
        {"name": "company_validate_scene_v1", "identity_arguments": "jsonb, boolean"},
        {"name": "validate_company_save_v1", "identity_arguments": "jsonb"},
        {"name": "reset_company_game", "identity_arguments": "uuid, text"}
      ],
      "probes": [
        "legacy_only_save_rejected",
        "canonical_without_legacy_scene_mirrors_accepted",
        "reset_returns_scene_v1"
      ]
    }
  }
}
